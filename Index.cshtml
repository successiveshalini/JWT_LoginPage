@model IEnumerable<MyWatch.Models.WatchDetails>
@{
    ViewData["Title"] = "Index";
}
<div class=" containet">
    <div class=" row">
        <div class="col-8 offset-2"></div>
        <h1 class="text-center text-primary ">Watch Details</h1>
        <a class=" btn btn-sm btn-success m-1" asp-controller="Watch" asp-action="Create">Create</a>
        <table class=" table table-bordered border-2">
            <tr class="text-center">
                <th>WatchId</th>
                <th>WatchName</th>
                <th>WatchModel</th>
                <th>WatchPrice</th>
                <th>WatchColor</th>
                <th>Buttons for Modify</th>

            </tr>
            @foreach (var d in Model)
            {
                <tr class="text-center">
                    <td>@d.WatchId</td>
                    <td>@d.WatchName</td>
                    <td>@d.WatchModel</td>
                    <td>@d.WatchPrice</td>
                    <td>@d.WatchColor</td>
                    <td>
                        <a class="btn btn-sm btn-warning" asp-controller="Watch" asp-action="Edit" asp-route-id="@d.WatchId">Edit</a>
                        <a class="btn btn-sm btn-danger" asp-controller="Watch" asp-action="Delete" asp-route-id="@d.WatchId">Delete</a>
                    </td>
                </tr>
            }

        </table>"
    </div>
</div>

